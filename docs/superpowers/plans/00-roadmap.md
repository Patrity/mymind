---
title: MyMind Master Roadmap
status: active
updated: 2026-08-15
read_first: true
---

# MyMind — Master Roadmap

The cross-cycle source of truth. **Read this first every session**, then the newest handover in [`../../handovers/`](../../handovers/).

MyMind is one modular Nuxt 4 service (web app + MCP server + HTTP endpoints) that consolidates Tony's document management, tasks/projects, image hosting, quick capture, memory, and clipboard into a single self-hosted "second brain," running in the homelab (Proxmox + Docker) and exposed to the internet for sharing.

It is built as a sequence of **sub-project cycles**. Each cycle: `brainstorm → spec → plan → build → handover → update roadmap + wiki`.

## Locked global decisions

These hold across all cycles. Change them here (with a dated note) if they ever change.

| Decision | Choice |
|---|---|
| Architecture | One Nuxt 4 service. Web + Nitro HTTP + MCP from the same app. No separate backends. |
| Memory system | **Reimplemented in Nitro/TS** (porting the proven Python `bridget-services/memory` design, not its code). |
| LLM/model access | **DB-backed registry (2026-06-10, cycle 12 — superseded env-only config).** Providers/models + per-usage failover chains live in one JSONB `ai_config` doc, edited in-app at `/settings`; keys encrypted at rest. Usages: `reasoning`, `bulk`, `embeddings`, `vision`/OCR, `stt`, `tts`, `rerank`. Providers are `anthropic` or `openai-compatible`. Local default (AI rig `192.168.2.25`), hosted (Haiku / GPT / Gemini Flash) for hard reasoning. Swapping a model = UI change, never code, never an env redeploy. The old `AI_*` env vars are now one-time onboarding import seeds only. |
| Embeddings | `qwen3-embedding-4b`, **2560-dim**, stored as `halfvec(2560)` with HNSW cosine. TEI fronted to expose OpenAI `/v1/embeddings`. |
| Doc organization | **Hybrid**: canonical path tree (incl. `/input` staging) for the human browser, plus first-class queryable columns (`project`, `domain`, `type`, `tags`, `topic` ltree) promoted out of frontmatter. |
| Search | Trigram (keyword) from cycle 1; semantic + RRF fusion added in the enrichment cycle. |
| Auth | better-auth. Two surfaces: session (web) + bearer API tokens (machine clients: ShareX, CC/Hermes hooks, MCP). |
| Storage | Local-disk/S3 abstraction (ported from `copipasta`). |
| Backups | Handled **out-of-band** (nightly DB dump to an external service). Not an in-app concern. In-app markdown export is an optional future feature, not core. |
| AI safety | Every AI mutation (tags, frontmatter, filing, memories) is reviewable/reversible via a `reviewed_at`-style surface. Auto-review high-confidence items; only low-confidence needs human review (2026-06-03). |
| Render mode (2026-06-03) | **SPA** (`ssr: false`) for the authed app; **SSR/prerender** only for public `/share/**` + `/i/**` via routeRules. Decided after the first-pass `/documents` pre-login flash. |
| Reranker (2026-06-03) | `Qwen3-Reranker-0.6B` at `192.168.2.25:8883` available for relevance scoring (memory/doc search). |

## Cycle status

Legend: `planned` → `spec'd` → `in-progress` → `shipped`

| # | Cycle | Status | Spec | Plan | Handover |
|---|---|---|---|---|---|
| 1 | **Foundation + Content Spine** — app shell, dual auth, Drizzle/pg/pgvector, storage, document model, path-tree browser + CodeMirror/MDC editor (edit/preview/split), manual frontmatter, trigram search, public-slug sharing, env provider scaffold. Ships a manual-but-complete doc manager. | ✅ shipped | [spec](../specs/2026-06-02-foundation-content-spine.md) | [plan](2026-06-02-foundation-content-spine.md) | [handover](../../handovers/2026-06-03-foundation-content-spine.md) |
| 2 | **AI Enrichment + Notification Queue** — embedding worker (fills `halfvec`), semantic + RRF search, `/input` auto-tag/sort/frontmatter, AI-action review surface, human-needed notification queue. (+ login page fast-follow) | ✅ shipped | [spec](../specs/2026-06-03-ai-enrichment.md) | [plan](2026-06-03-ai-enrichment.md) | [handover](../../handovers/2026-06-03-ai-enrichment.md) |
| 3 | **Quick Capture + Image Hosting/Gallery** — quick note/todo/idea capture, image/gif/video upload (ShareX/CleanShot endpoints), sharp→webp/webm, OCR tags (confirmed + recommended), gallery, public/private. | ✅ shipped | [spec](../specs/2026-06-03-capture-images.md) | [plan](2026-06-03-capture-images.md) | [handover](../../handovers/2026-06-03-capture-images.md) |
| 4 | **Tasks + Projects (Kanban)** — projects (name/desc/active), kanban (todo/in-progress/completed/blocked), task fields + audit log, doc↔project↔domain relations. | ✅ shipped | [spec](../specs/2026-06-03-tasks-projects.md) | [plan](2026-06-03-tasks-projects.md) | [handover](../../handovers/2026-06-03-tasks-projects.md) |
| 5 | **Memory + MCP Server + Hook Endpoints** — mem schema, hybrid search, enrichment loop (env provider), HTTP hooks for CC/Hermes, MCP tools (memories/docs/projects/tasks), scheduler tasks, GitHub-commit→memory. Deprecates the Python service. | ✅ shipped | [spec](../specs/2026-06-03-memory-mcp.md) | [plan](2026-06-03-memory-mcp.md) | [handover](../../handovers/2026-06-03-memory-mcp.md) |
| 6 | **Clipboard** — port `copipasta` as a page (self-contained; can slot in anytime). | ✅ shipped | [spec](../specs/2026-06-03-clipboard.md) | [plan](2026-06-03-clipboard.md) | [handover](../../handovers/2026-06-03-clipboard.md) |

### Round 2 — feedback after first-pass acceptance

| # | Cycle | Status | Spec | Plan | Handover |
|---|---|---|---|---|---|
| 7 | **Backend fixes & AI quality** — OCR retry-loop fix, tag cap (5–7), md-first transcription + title inference, memory auto-review threshold + relevance scores + reviewed-tag removal. | ✅ shipped | [spec](../specs/2026-06-03-backend-fixes.md) | [plan](2026-06-03-backend-fixes.md) | [handover](../../handovers/2026-06-03-backend-fixes.md) |
| 8 | **Global UX & architecture** — SPA conversion (SSR only for public pages), command palette (`UDashboardSearch`) with semantic search across docs/memories/gallery/tasks. | ✅ shipped | [spec](../specs/2026-06-03-spa-search.md) | [plan](2026-06-03-spa-search.md) | [handover](../../handovers/2026-06-03-spa-search.md) |
| 9 | **Documents power-editor** — custom MDC components + markdown toolbar (.md only), inline image paste→upload→embed, drag-drop move, context menu (rename/move/share/delete), copy-link, last-open cookie. | ✅ shipped | [spec](../specs/2026-06-03-doc-editor.md) | [plan](2026-06-03-doc-editor.md) | [handover](../../handovers/2026-06-03-doc-editor.md) |
| 10 | **Interaction polish** — Capture (paste/camera/drag-drop), Gallery (paste/DnD/video/filetype/search+tag-filter), Tasks (drag-drop + project/priority filters), Memories (add modal + tag filter), Clipboard (machine attribution). | ✅ shipped | [spec](../specs/2026-06-03-interaction-polish.md) | [plan](2026-06-03-interaction-polish.md) | [handover](../../handovers/2026-06-03-interaction-polish.md) |
| 11 | **Sessions view** — browse raw CC/Hermes transcripts + token usage / message count / tool-use stats. | ✅ shipped | [spec](../specs/2026-06-03-sessions-view.md) | [plan](2026-06-03-sessions-view.md) | [handover](../../handovers/2026-06-03-sessions-view.md) |

> Cycle ordering reflects dependencies: the spine underpins everything; enrichment makes it smart; capture/images/tasks are features on the spine; memory depends on docs/projects/tasks existing as MCP targets; clipboard is independent. Round 2 = polish/fixes on the shipped base; backend fixes (7) first, then architecture (8) before the UI-heavy editor/interaction work (9–10).

### Round 3 — planned (new scope; see [`docs/BACKLOG.md`](../../BACKLOG.md) for detail + open items)

| # | Cycle | Status | Spec | Plan | Handover |
|---|---|---|---|---|---|
| 12 | **AI model/provider registry** — DB-backed providers/models + per-usage failover-chain assignments in one JSONB `ai_config` doc; encrypted keys at rest (`CONFIG_ENC_KEY`, HKDF-from-`BETTER_AUTH_SECRET` fallback); `/settings` (3 tabs) + `/onboarding` wizard + redirect middleware; resolver hands decrypted failover chains to all AI consumers. Embeddings fixed at 2560-dim with a save-time dim-probe. The old `AI_*` env vars are now import-only seeds, not runtime config. **Shipped + live E2E green + verified on deployment (2026-06-11); merged to master.** | ✅ shipped | [spec](../specs/2026-06-10-ai-config-registry-design.md) | [backend](2026-06-10-ai-config-registry-backend.md) · [ui](2026-06-10-ai-config-registry-ui.md) | [handover](../../handovers/2026-06-10-ai-config-registry.md) |
| 13 | **Bridget Parity** (broadened from "API key management UI") — replaces the bridget memory service end-to-end, in 5 phases: (1) API-key CRUD UI + Connect-to-Claude-Code (served `cc-hook.sh` + MCP/hooks snippets); (2) capture-fidelity ingestion (`tool_events` table, message thinking/sidechain/model/parent/usage, session git/machine/app/ended); (3) one-time raw import of **457 claude_code sessions / 96.7K msgs / 42.4K tool events** (no memories — re-enriched locally); (4) session summarization worker + session/message semantic search (RRF, in the palette); (5) enrichment tuning + memory intelligence — rich provenance + a `memory_relations` graph + an LLM relationship-judge with auto-supersede + review-gated contradictions. Migrations 0015-0018. **Shipped on `feat/bridget-parity` (NOT yet merged); gates green (typecheck/test=315/build); live-validated on the dev rigs (2026-06-16) — incl. 6 auto-supersessions + provenance over real imported data.** Subsumes the backlog's session-summarization, bridget-migration, and session/message-search items. | ✅ shipped | [spec](../specs/2026-06-15-bridget-parity-design.md) | [p1](2026-06-15-bridget-parity-phase1-tokens-connect.md)·[p2](2026-06-15-bridget-parity-phase2-capture-fidelity.md)·[p3](2026-06-15-bridget-parity-phase3-import.md)·[p4](2026-06-16-bridget-parity-phase4-summaries-search.md)·[p5](2026-06-16-bridget-parity-phase5-memory-intelligence.md) | [handover](../../handovers/2026-06-16-bridget-parity.md) |
| 14 | **In-app AI chat** — reasoning chat over docs/memories/projects/tasks via server-side tool-calling (reuses the MCP tool surface); cites sources, takes confirmed actions. Note: the shared agent core + text-chat endpoint (`/api/agent/chat`) shipped in cycle 17 — a full chat UI is what remains. | planned | — | — | — |
| 15 | **Capture/OCR robustness** — explicit dedup (untagged-only) + transcription retry (backoff / manual / auto-on-recovery) + surface OCR-failed & ambiguous-project to the notification queue. | planned | — | — | — |
| 16 | **CD → homelab Proxmox LXC** — single `deploy.yml` workflow: `test` job (lint non-blocking / typecheck / test / build) on every push gates a `deploy` job that runs on a self-hosted runner on the Proxmox host `mini`, tar-pipes the checkout into LXC 114 `/opt/mymind`, and `docker compose up -d --build` (migrate-on-start via Dockerfile CMD) + `/login` health check. Docs-only pushes skipped (`paths-ignore`). Chose the self-hosted-runner variant (LAN-only app box) over pull-based/SSH. **Shipped + live E2E green; verified on box (2026-06-10).** | ✅ shipped | [spec](../specs/2026-06-09-ci-deploy-pipeline-design.md) | [plan](2026-06-09-ci-deploy-pipeline.md) | [handover](../../handovers/2026-06-09-ci-deploy-pipeline.md) |
| 17 | **Voice Agent ("Jarvis")** — `/voice` page; Unmute STT/TTS with barge-in re-pointed at a Nitro agent loop (`/api/agent/llm`, OpenAI-spec); shared tool registry (11 tools) used by voice, MCP, and future text-chat; Three.js reactor; universal undo. Subsumes cycle 14 agent core. **Superseded by cycle 18.** | ✅ shipped | [spec](../specs/2026-06-08-voice-agent-jarvis-design.md) | [plan](2026-06-08-voice-agent-jarvis.md) | [handover](../../handovers/2026-06-08-voice-agent.md) |
| 18 | **Voice Agent v2 (self-hosted, self-orchestrated)** — Replaced Unmute with a fully owned TS pipeline: Silero VAD + WAV barge-in (client), Nitro WS orchestrator, swappable OpenAI-spec STT (faster-whisper) + TTS (Kokoro/Chatterbox) providers, Vercel AI SDK `runAgent` core shared by voice + chat + cron. Unmute stack removed. | ✅ shipped | [spec](../specs/2026-06-09-voice-self-hosted-redesign-design.md) | — | [handover](../../handovers/2026-06-09-voice-v2.md) |
| 19 | **Voice Visualizer Redesign** — Replaced placeholder wireframe icosahedron + 48-point ring with a GPU-particle Three.js visualizer: 50k-particle sphere core (GLSL vertex-shader motion), 96-bar instanced mic-frequency ring, tool pulse rings + transcription sparks. Pure-TS choreographer (14 unit tests) drives all state transitions and event impulses (barge-in shatter, error shockwave, sttFinal sparks, connect assembly). Quality tiers + FPS watchdog + context-loss rebuild + CSS fallback. `useVoice` gains `connecting`/`tool` states and `onVizEvent` emitter. Manual desktop+phone tuning pass pending. | ✅ shipped | [spec](../specs/2026-06-09-voice-visualizer-redesign-design.md) | [plan](2026-06-09-voice-visualizer-redesign.md) | [handover](../../handovers/2026-06-10-voice-visualizer.md) |
| 20 | **Image Enrichment Pipeline** — Replaced the ad-hoc image OCR cron with a status-driven enrichment pipeline (`enrich_status` pending→processing→done\|failed, retry-capped at 3): one unified vision pass per image (`describeImageFull` → strict-JSON summary + verbatim Markdown OCR + 5–7 tags), a summary embedding (`halfvec(2560)`, HNSW cosine) making images first-class searchable via **hybrid trigram + summary-vector RRF** (`searchImages`), library-based tag auto-apply (known→`tags`, new→`recommended_tags`), optional document spin-off (`documents.ocr_id` + "View source image" / `?image=` deep-link), and a fully editable gallery metadata surface (summary/ocr/tags + Reprocess vs Revectorize). Cron `enrich-images` `*/7 * * * *`; shared core also drives admin ocr-run + images-backfill. `embedding` is server-only (redacted from `ImageDTO`). **Shipped + gates green (typecheck/test=212/build/migrate) + live vision/embedding E2E PASS (2026-06-12, full stack via the real rigs). Note: reassign the `vision` usage to the `:8005` model in `/settings` — it's currently on a reasoning MoE.** | ✅ shipped | [spec](../specs/2026-06-11-image-pipeline-design.md) | [plan](2026-06-11-image-pipeline.md) | [handover](../../handovers/2026-06-11-image-pipeline.md) |
| 21 | **Live Reactivity** — Made the frontend live across devices/tabs. One in-process event bus (`publishChange({resource,action,id})`) → one auth-gated SSE stream `/api/events` → one `EventSource` per tab → `@tanstack/vue-query` cache invalidation → targeted refetch. Thin signals (no payload), single global channel (single-user), no Redis/CRDT/DB-triggers/replay-log (reconnect self-heals via `refetchOnReconnect`). Every Nitro writer (HTTP handlers + background tasks: image-enrich, embedding, memory-enrich, enrich-input) now publishes; all list/detail surfaces (gallery, doc tree+detail, memories, review, projects, tasks kanban, sessions) migrated to vue-query; sidebar badges event-driven (60s poll removed). Drag-guarded tasks board + dirty-gated doc editor. Convention enforced by `.claude/rules/live-data.md` + `add-live-resource` skill + the closed `ResourceName` union. **Shipped + gates green (typecheck/build/test=219) + plugin-init/auth-gating verified on prod preview + user-validated live cross-tab images PASS (2026-06-12). Full multi-resource E2E sweep recommended next.** | ✅ shipped | [spec](../specs/2026-06-12-live-reactivity-design.md) | [plan](2026-06-12-live-reactivity.md) | [handover](../../handovers/2026-06-12-live-reactivity.md) |
| 22 | **Activity Log (Observability)** — One `activity_log` table + a self-isolating recorder (`withSpan`/`recordEvent`, AsyncLocalStorage trace/parent correlation, buffered fire-and-forget flush, redaction at the choke point). Six emit seams (model failover attempts, agent reasoning, agent tools, the 4 cron jobs + summaries, enrichment structured errors replacing swallowed `console.warn`, inbound API requests). `/activity` live list (true pause) + nested trace-tree detail + ack/ack-all, riding the cycle-21 live channel (`activity` `ResourceName`). Severity-tiered daily prune (14d/90d/500k). Three configurable alert channels — sidebar badge + new-error toast + **Resend email** (severity-gated, windowed digest) — edited in a `/settings → Activity & Alerts` tab. Stands up Resend (closes the Email backlog item). **Shipped + gates green (typecheck/build/test=250); live E2E with the rigs pending user acceptance. Deferred: model request/response *body* capture + happy-path `model` parent span (fast-follow; redaction plumbing already in place).** | ✅ shipped | [spec](../specs/2026-06-15-activity-log-observability-design.md) | [plan](2026-06-15-activity-log-observability.md) | [handover](../../handovers/2026-06-15-activity-log.md) |
| 23 | **Project-Association Foundation (Phase 1)** — Canonical `projects` entities keyed on the normalized **git remote**. `findOrCreateProject` (ssh↔https/`.git`/port normalized → match `git_remote_key` → `aliases @>` → race-safe create → seeded **Uncategorized** for no-remote). Migration 0019 rewrites `projects` to a surrogate **uuid `id` PK** (slug stays unique) + `git_remote_key` (partial-unique), repo/prod/staging URLs, `aliases[]`, `local_paths[]`, `details jsonb`, `last_activity_at` (`active` boolean KEPT — spec deviation, user-approved). Session ingest resolves `sessions.project_id` on the event path; memory enrichment sets `memories.project_id` **by scope** (agent→session project, user/world→null) + `memories.source_date` (source session `started_at`, "last observed", `greatest`-merged); dedup + candidate selector now bucket on `project_id`. Idempotent backfill `scripts/backfill-projects.ts` (dev: 463/463 sessions resolved, real git-keyed projects materialized, 1628/1633 memories dated). **Built subagent-driven on `feat/project-association`; gates green (typecheck/test=320/build); final integration review READY-TO-MERGE. `enrich-memories` cron held disabled until prod backfill runs. Phases 2-3 (merge, doc/task assoc, auto-move, UI) deferred.** | ✅ shipped | [spec](../specs/2026-06-16-project-association-foundation-design.md) | [plan](2026-06-16-project-association-foundation.md) | [handover](../../handovers/2026-06-16-project-association-foundation.md) |
| 24 | **Sessions UX + SSE** — Split the session-detail endpoint into **meta** (`GET /api/sessions/[id]` → `getSessionMeta`) + **messages** (`GET /api/sessions/[id]/messages?since=` → `getSessionMessages`); the detail page is a **resizable split-pane** (metadata left, transcript right) that loads progressively (`useSessionMeta` instant + `useSessionMessages`), with a **virtualized** transcript (`@vueuse useVirtualList` — a 497-msg session mounts ~16 DOM rows) that **autoscrolls + live-tails** (watches `meta.messageCount`, fetches `?since=` deltas, appends with tool-event de-dup; "↓ N new" button + viewport-sized at-bottom threshold). List shows a per-row activity pulse. Pure helpers `transcript-scroll.ts` (`isAtBottom`/`countNewSince`) unit-tested. **Built subagent-driven on `feat/sessions-ux`, merged to local master; gates green (typecheck/test=322/build); playwright-validated; final review READY-TO-MERGE.** Spec note: live-dispatch already covered sessions (no handler added). | ✅ shipped | [spec](../specs/2026-06-16-sessions-ux-sse-design.md) | [plan](2026-06-16-sessions-ux-sse.md) | [handover](../../handovers/2026-06-16-sessions-ux-sse.md) |
| 25 | **Projects UI + per-project color** — `/projects` reworked to surface the full cycle-23 model (git remote, repo/prod/staging URLs, aliases, local_paths, **session/memory counts**) + an Edit modal (URL fields, aliases `UInputTags`, colour swatch picker). Per-project `color` (migration 0020, nullable; **grey `NEUTRAL_COLOR` default**, 14-hue palette opt-in). Reusable **`<ProjectBadge>`** (coloured pill) resolves colour via explicit prop → shared cached `useProjectColors()` map → grey default, used on memories/sessions/tasks. `findOrCreateProject` now also **matches `slug`/`aliases` by cwd label** (friendly "Bridget Services" ← alias `bridget-services`; match-only, no create). **Built subagent-driven on `feat/projects-ui-color`, merged to master; gates green (typecheck/test=326/build); playwright-validated.** Follow-up **shipped** (`feat/project-dashboard`, 2026-06-16): the `/projects/[slug]` **dashboard** (header + stats + Sessions/Tasks/Memories tabs) + row-click nav + **editable slug with transactional cascade rename** + counts-by-slug fix + two latent `ProjectBadge` link bugs fixed (string-`:is` → `resolveComponent`; `string\|false` boolean-cast → `string\|null`) — gates green (typecheck/test=336/build), playwright-validated, final review READY-TO-MERGE; see [handover](../../handovers/2026-06-16-project-dashboard.md). Still deferred: project merge (phase 3). | ✅ shipped | [spec](../specs/) | [plan](2026-06-16-projects-ui-color.md) · [dashboard](2026-06-16-project-dashboard.md) | [handover](../../handovers/2026-06-16-projects-ui-color.md) · [dashboard](../../handovers/2026-06-16-project-dashboard.md) |
| 26 | **Document↔Project Association (Phase 2)** — Documents associate with canonical projects by a **path⟺project invariant**: a doc is associated to X iff its `documents.path` is under `/projects/<slug>/` (lowercase doc-tree path). Migration 0021 adds `documents.project_id` (uuid FK + backfill); a single choke point (`createDoc`/`updateDoc` → `resolveDocProjectFromPath`) derives `project_id`+slug from the FINAL path on every write (path always wins; passing `project=X` **relocates** the doc = assign-project). Three triggers: manual move, assign-project, and the `/input` enrichment classify (`buildEnrichMessages` feeds the project list → proposer picks a real slug + `/projects/<slug>/` path → rides the existing review→approve flow). The editable-slug rename cascade extended to rewrite associated doc paths. Surfaced: `documentCount` (+ a fix making task/memory counts exclude soft-deleted/archived so stats match tabs), `listDocs`/`?project=`, `useDocList`, a **Documents tab** on the dashboard, and the OCR→doc spin-off now emits a `document` live event. Tasks were already associated (out of scope). **Built subagent-driven (6 tasks, two-stage review + final opus review: ship-it); playwright E2E PASS (create/move/assign all associate; count matches tab; slug rename cascades doc paths; soft-delete drops count). Real suite=379 (vitest now excludes `.claude/**`).** | ✅ shipped | [spec](../specs/2026-06-17-document-project-association-design.md) | [plan](2026-06-17-document-project-association.md) | [handover](../../handovers/2026-06-17-document-project-association.md) |
| 27 | **Project Merge (Phase 3)** — Fold a legacy duplicate project (loser) into its canonical git-keyed twin (winner) and delete the dupe. `mergeProjects(loser,winner)` in one transaction: guards (self/uncategorized/not-found); repoint `sessions`/`memories` (by `project_id` OR denormalized slug) + `tasks` (slug-only) + **documents row-by-row** (path `/projects/L/`→`/projects/W/` with **collision uniquify**); absorb loser slug+aliases(+gitRemoteKey) into the winner so re-ingest resolves to W; **hard-delete** the loser. Post-merge **memory dedup** reuses `createMemory`'s extracted `buildDedupCandidates`+`dedupDecision` (sequential collapse). `POST /api/projects/[slug]/merge` + a dashboard **"Merge into…"** confirm dialog (target picker, preview counts, destructive warning) → navigates to the winner. **Built subagent-driven (5 tasks, two-stage review + final opus review: ship-it); playwright E2E PASS (repoint + doc-collision uniquify + alias absorb + loser 404 + near-dup memory collapse). No migration (pure logic + UI). typecheck 0 / test 393 / build.** Note: the 3 `project_id` FKs exist in prod but aren't modeled in the Drizzle schema (review any `db:generate` touching them). | ✅ shipped | [spec](../specs/2026-06-17-project-merge-design.md) | [plan](2026-06-17-project-merge.md) | [handover](../../handovers/2026-06-17-project-merge.md) |

| 28 | **Unified Agent Surface (`/agent`) + Conversation Persistence + Bridget** — Merged `/voice`→`/agent` (toggleable visualizer); one WS for talk+type with **`speak` as the sole voice/text branch** (gates TTS + prompt mode; new `typing` viz state); profile-aware `runAgent` (`AgentProfile`/`bridgetProfile` — the seam for Cycle B). New modality-agnostic `conversations`/`conversation_messages` store (migration 0022; tree-capable `parent_id` **linear**; `summary_embedding` reserved) persisting every turn; `/agent/history` keyword search + resume + `/agent?c=` deep-link. Real Bridget personality: editable persona (`settings.agent_persona`, `/settings → Bridget`) + time-of-day tone + once-per-connection live context (projects/tasks). Mic **decoupled** from the WS (typing never prompts for a mic). SSE `/api/agent/chat` kept headless-only. **Built subagent-driven (11 tasks); gates green (typecheck 0 / test 399 / build / migrate 0022); playwright E2E PASS on dev+rig (full turn "Evening, Tony!", persistence, resume, search, persona) — caught+fixed a Nuxt nested-route bug.** Powerful capability tools (web research/shell/SSH/`gh`/file-edit) = separate **Cycle B**. | ✅ shipped | [spec](../specs/2026-06-17-agent-surface-chat-design.md) | [plan](2026-06-17-agent-surface-chat.md) | [handover](../../handovers/2026-06-17-agent-surface-chat.md) |

| 29 | **Web Research for Bridget (Cycle B, Phase 1)** — `web_search` + `web_fetch` read-only tools on Bridget's **default** toolset (no new profile/gate — Cycle B2 brings the approval harness). Native tools in one loop. Pluggable `SearchProvider` interface (SearXNG + Brave impls); encrypted `search_config` doc (mirrors ai-config registry); zero-config SearXNG default. SSRF-guarded `fetchAsMarkdown` (private/loopback/link-local/CGNAT + IPv4-mapped `::ffff:` + per-hop redirect re-guard; 2 real holes found+fixed in review). Research prompt guidance: cite sources as markdown links; treat web content as untrusted. SearXNG **bundled** in `docker-compose.prod.yml` (internal-only, no host port). `/settings → Search` tab. **Built subagent-driven (6 tasks, two-stage review); gates green (typecheck 0 / test green / build); `web_fetch` live PASS (`example.com` → correct extraction + markdown citation); SSRF refused live + 10/10 unit; `web_search` deploy-pending (no bundled SearXNG in dev; normalizers 4/4 unit-tested).** B2 (gate/exec), B3 (gh/file-edit), B4 (ssh) remain. | ✅ shipped | [spec](../specs/2026-06-17-web-research-b1-design.md) | [plan](2026-06-17-web-research-b1.md) | [handover](../../handovers/2026-06-17-web-research-b1.md) |
| 30 | **Approval-Gate Harness + Constrained Exec (Cycle B2)** — The security keystone of Cycle B. A human-in-the-loop approval gate pauses every dangerous tool call until Tony approves or denies it in the UI, with a persisted glob-pattern allowlist (`exec_approvals`, migration 0024) that bypasses the prompt for pre-approved command shapes. A `powerful` `AgentProfile` (per-connection toggle, defaults safe) gates access to the new `exec` tool, which runs shell commands via `setpriv --reuid --regid --clear-groups` (full privilege drop to uid 10001), a `/workspace` cwd-jail, a secrets-stripped allowlist-constructed env, and a 60 s timeout/64 KB output cap. Fails closed — can never run as the app user. The `Dockerfile` installs `util-linux` (setpriv), creates the `agent` user (uid 10001), and sets the workspace envs; `docker-compose.prod.yml` mounts a persistent `mymind-workspace` volume. The `/settings → Agent Tools` tab manages allowlist patterns (add/edit/revoke). The `ApprovalPrompt` component renders inline in the transcript. **Gates: typecheck 0 · test 491/491 (79 files) · build clean · migration 0024 applies on deploy. Playwright E2E pending (controller-run post-docs).** | ✅ shipped | [spec](../specs/2026-06-17-exec-approval-gate-b2-design.md) | [plan](2026-06-17-exec-approval-gate-b2.md) | [handover](../../handovers/2026-06-18-exec-approval-gate.md) |
| 31 | **Document Chunking + Contextual Retrieval** — Replaced whole-doc embedding (docs over TEI's 16k limit were silently dropped from semantic search) with a generic per-**`chunk`** index (migration 0023; `source_type` document\|image; structure-aware markdown chunker ~300 tok / 512 cap; resilient LLM contextual prefixes; `halfvec(2560)` HNSW cosine). `chunkAndEmbedSource` (delete-then-insert), `runEmbedding` re-chunks on `chunked_hash` drift. Chunk-aware `searchDocs` (best-chunk-per-doc collapse → RRF) + new `searchPassages` / MCP `search_passages`; long image OCR chunked too. **Shipped + merged; typecheck 0 / test 428 / build / migrate 0023.** (Cycle 30 = parallel exec-gate work on `feat/exec-gate`.) | ✅ shipped | [spec](../specs/2026-06-17-document-chunking-contextual-retrieval-design.md) | [plan](2026-06-17-document-chunking-contextual-retrieval.md) | [handover](../../handovers/2026-06-18-document-chunking.md) |
| 32 | **Search Relevance + Command-Palette UX** — Cross-type reranker over fused candidates + absolute cutoff in `searchAll` (ONE rerank call; **raw** scores — min-max dropped; exact-substring **pin**); always-on per-lane **cosine floor** (`search_relevance` config key); unified `{ hits, reranked }` contract replacing the 7 fixed groups; matched-passage **snippets** + query highlight; **Option-A palette** ("Top results" + per-type groups, score badges when reranked). Pure unit-tested helpers (`rankCandidates`/`makeSnippet`/`highlightTokens`/`collapseChunksToHits`/`getSearchConfig`/`parseRerankResponse`). **Built subagent-driven (10 tasks, two-verdict review + opus whole-branch); typecheck 0 / test 451 / build; live E2E (playwright) PASS for contract/fallback/reranked-path/UI. ⚠️ reranker (`:8883`) is Cohere-shim-compatible (the `model` field is ignored) but has a LENGTH-DEPENDENT score scale → the shipped fixed cutoff is fragile; keep the `rerank` usage UNASSIGNED until the cutoff is reworked to relative/top-k (task 96ffb5bb; optional 4B reranker, VRAM-permitting). Earlier "miscalibrated / gibberish→0.999" notes were test artifacts, retracted (corrected 2026-06-19).** Subsumes reranker-enablement task e356a621. | ✅ shipped | [spec](../specs/2026-06-18-search-relevance-palette-ux-design.md) | [plan](2026-06-18-search-relevance-palette-ux.md) | [handover](../../handovers/2026-06-18-search-relevance-palette-ux.md) |
| 33 | **Relative Rerank Cutoff** — Replaced cycle-32's fixed absolute `rerankCutoff` (fragile under the reranker's verified LENGTH-DEPENDENT score scale) with a per-query **top-k + relative band** in `rankCandidates`: reranked hits kept iff `lexicalExact \|\| score >= rerankRelBand × topScore`, capped to `rerankTopK` (config drops `rerankCutoff`, adds `rerankTopK: 12` / `rerankRelBand: 0.6`). Anchored to the query's own top → scale-robust + robust to the false-positive scatter. Empty state is now retrieval-based (a non-empty pool always yields ≥1 hit; no score-gating). Pure-logic, 2 tasks, inline TDD. **typecheck 0 / test 455 / build.** Companion infra (homelab): reranker upgraded 0.6B (inverted technical queries) → **`mxbai-rerank-large-v2`** (1.5B, fp16) — verified to rank correctly (Intel X550 → relevant 1.0, length-robust). Reranker still UNASSIGNED in prod pending live validation + tuning on enable (task 96ffb5bb). | ✅ shipped | [spec](../specs/2026-06-19-relative-rerank-cutoff-design.md) | [plan](2026-06-19-relative-rerank-cutoff.md) | [handover](../../handovers/2026-06-19-relative-rerank-cutoff.md) |
| 34 | **Native LXC Deploy (Cycle B3.1)** — Re-platformed the app from a Docker container to a native **systemd** service (`deploy/mymind.service`, run as **root**) in LXC 114 so the agent's `exec` runs natively in the LXC (prerequisite for B3.2). `docker-compose.prod.yml` reduced to **db + searxng** (Postgres published `127.0.0.1:5432`; **corpus volume untouched**); native CD (`deploy.yml`): idempotent `provision-native.sh` (Node 22/pnpm, `.env.native`, **uploads-volume → native-dir migration**, unit install) → **build-before-cutover** (old app serves during build → no outage if build fails) → `systemctl restart mymind` → health-check. Dockerfile retired. **Shipped + DEPLOYED to prod (master 2ff6fc0); first attempt failed SAFE (native build OOM'd at V8's 2GB default → fixed with `--max-old-space-size=4096`; old app kept serving), re-deploy green (cutover + login-200).** Interim: `exec` returns `disabled` (fail-closed) until B3.2 reworks the runner native. Pending: Tony's box-level validation (uploads loaded, restart-survives). | ✅ shipped | [spec](../specs/2026-06-19-native-lxc-deploy-b3-1-design.md) | [plan](2026-06-19-native-lxc-deploy-b3-1.md) | [handover](../../handovers/2026-06-19-native-lxc-deploy.md) |
| 35 | **Credentialed, Self-Installing Native Exec (Cycle B3.2)** — Turned `exec` back ON for the native LXC. Runs as **root in LXC 114** (no setpriv/uid-10001/`/workspace` jail; `/bin/sh -c`, default cwd `/opt/mymind/workspace`). **Always-on credential injection** (encrypted `exec_secrets` settings store reusing ai-config AES-256-GCM; every stored token → env var; `/settings → Secrets` write-only tab + REST). **Allowlist-first gate** (`autoApprove` skips the prompt for matched/LAN commands; catastrophic **hard-block** before spawn). **Outbound policy**: LAN always-allowed; external auto-approves only if every host ∈ approved `host:<hostname>` set. **Dual enable** (opt-in `powerful` profile AND `agent-exec-enabled` cookie; both default off; off for unattended runs via `effectiveTools` filter). **Audit**: `activity_log` tool span + `maskSecrets` value redaction (result + logged command). Built subagent-driven (8 tasks, two-verdict review + opus whole-branch). **typecheck 0 / test 588 / build; DEPLOYED to prod (master 5a4dd87, run green).** ⚠️ **ACCEPTED RISK (Tony):** external-outbound auto-approve is static-analysis-based and NOT exfil-proof (proxy/`--connect-to`/`--resolve` flags + shell-chaining bypass once a host is allowlisted) — accepted (LXC=boundary, opt-in+audited); documented in wiki "Accepted limitations". **Tony's credentialed interactive E2E VERIFIED (2026-06-22): gh private-repo via injected token + token redaction confirmed — cycle closed.** | ✅ shipped | [spec](../specs/2026-06-20-credentialed-native-exec-b3-2-design.md) | [plan](2026-06-20-credentialed-native-exec-b3-2.md) | [handover](../../handovers/2026-06-20-credentialed-native-exec-b3-2.md) |
| 36 | **Image Generation Tool (`generate_image`)** — A `generate_image` agent tool (ComfyUI + Qwen-Image) that renders an image from a text prompt, persists it into the gallery (vision-enrich skipped; prompt = summary + embedding source; tag `['generated']`; `enrich_status: done`), and is auto-exposed via MCP. Config lives in a DB `image_config` settings doc edited at `/settings → Image Gen` (NOT the `ai_config` registry; no migration). Synchronous execution (~1 min/image, 180 s cap, honors abort signal). Parameters: `prompt`, `negative_prompt`, `width`, `height`, `steps`, `cfg`, `seed` (deterministic), `n` (1–4). Fails clean — never throws (`{ ok:false, error }` when ComfyUI unreachable). Built subagent-driven (8 tasks); **typecheck 0 / test 620 (94 files) / build.** Deferred: live diffusion-preview WebSocket stream; REST `POST /api/images/generate` endpoint. | ✅ shipped | [spec](../specs/2026-06-22-generate-image-tool-design.md) | [plan](2026-06-22-generate-image-tool.md) | [handover](../../handovers/2026-06-24-generate-image-tool.md) |
| 37 | **Image Editing Tool (`edit_image`) + Reliable Render (Phase 1)** — Phase 1 of img2img editing. New `edit_image` agent tool (ComfyUI img2img on the Qwen-Image model): describe the change; defaults to the most recently generated image (`resolveSourceImageId`); `strength`/denoise controls departure from source; tagged `['generated','edited']`; auto-exposed via MCP (tool surface 19→20). `image_config` gains `editStrength`. **Reliable render fix:** the model no longer receives an image URL — the orchestrator intercepts a `display` sentinel from the tool result, looks up the real persisted row, and authors the chat embed itself; the model only sees `{ ok, id, summary }`. This supersedes the cycle-36 markdown-paste approach and closes a hallucination-render bug. Built subagent-driven (8 tasks); **typecheck 0 / test 642 (100 files) / build clean.** Deferred: Phase 2 (paste-upload source), Phase 3 (mask/inpaint), Qwen-Image-Edit model, lineage chain UI. | ✅ shipped | [spec](../specs/2026-06-25-image-edit-img2img-design.md) | [plan](2026-06-25-image-edit-img2img.md) | [handover](../../handovers/2026-06-25-image-edit-img2img.md) |
| 38 | **Qwen-Image-Edit-2509 Instruction Editing (`edit_image` cycle 2)** — Replaced the cycle-37 img2img approach with **Qwen-Image-Edit-2509**, an instruction-tuned diffusion model. The tool now takes a targeted instruction prompt ("change the hat to a blue cowboy hat") and edits the named region while preserving the rest, reusing the encoder/VAE of the source. **img2img + `editStrength`/`strength` removed.** Fast merged **4-step default** (~14 s); `quality: true` flag selects 20-step unmerged path (slower, sharper). `FluxKontextImageScale` auto-resolution. Reliable-render path (cycle 37, model never gets the URL; orchestrator authors the embed) is intact. `image_config` gains Qwen-Image-Edit-2509 model name + graph IDs; `editStrength` dropped. Built subagent-driven (7 tasks); **typecheck 0 / test 647 (100 files) / build clean.** Deferred: paste-upload source (Phase 2), mask/inpaint (Phase 3), lineage chain UI, multi-image. | ✅ shipped | [spec](../specs/2026-06-26-qwen-image-edit-design.md) | [plan](2026-06-26-qwen-image-edit.md) | [handover](../../handovers/2026-06-26-qwen-image-edit.md) |
| 39 | **Multimodal Agent (image + file attachments)** — Attach images + files to `/agent` (paste/drag/picker); the natively-multimodal reasoning model (**Qwen3.6-35B-A3B VLM**) sees them as message **content parts** and decides from the prompt — reason over them, or call `edit_image` on an attached photo. **Key constraint:** vLLM forwards `image_url`/video/audio parts ONLY (no file part), so files become images the VLM can SEE: **PDFs render to page images** (`pdf-to-img`+`sharp`→webp, cap 8pg/1600px), text-like files → text parts, other binary rejected at the composer. `AgentContentPart` = text\|image only (no file part); model gets BYTES inline (base64 data-URLs), never auth-gated URLs. `edit_image` defaults its source to the turn's attachment (`resolveSourceImageId` preferIds). New `agent_files` table + `conversation_messages.attachments` jsonb (**migration 0025**); attachments persist + re-render in the transcript (thumbnails / file chips) live + on reload. Cycle-37/38 reliable-render (generated images: no URL to model) intact — attachments are a separate INPUT path. v1 sends attachment content for the CURRENT turn only (`getAgentHistory` stays text-only). Built subagent-driven (9 tasks, per-task two-verdict review); **typecheck 0 / test 671 (107 files) / build clean.** Deferred: multi-turn image memory, office docs (docx/xlsx→pdf), audio/video, PDF page thumbnails in transcript, render caps in /settings, file→doc-library ingestion, AI SDK v7. **✅ ACCEPTED (Tony, confirmed 2026-08-05): merged to master `c660078`, deployed (CD 28366761446 + post-ship fix 28484922407), and the live prod `/agent` re-test passed — the `[image]` imitation bug is dead.** The underlying blindness it exposed (the model never sees its own tool calls in history) is tracked separately as cycle 43 / task `5cc52941`. | ✅ shipped | [spec](../specs/2026-06-28-multimodal-agent-design.md) | [plan](2026-06-28-multimodal-agent.md) | [handover](../../handovers/2026-06-29-multimodal-agent.md) |
| 40 | **Agent-Usable Document Tools** — 9 new agent/MCP tools (tool surface 20→29): read/grep tools for long-doc-friendly navigation (`read_document` outline map + section/range read; `grep_document` substring/regex search); surgical edit tools (`edit_document` exact find/replace; `edit_section` append/replace-section; `update_document` whole-content + metadata; `move_document`); delete tools (`delete_document`, `delete_task`, `forget_memory`) completing the CRUD triad. All backed by a new pure `server/lib/documents/edit-ops.ts` module (26 unit tests, zero DB). Three restore helpers for undo contracts. Server-level MCP `instructions` preamble (second-brain workflow). Description re-voice on memory/doc/task tools. No migration. Built subagent-driven (7 impl tasks); **typecheck 0 / test 701 (109 files) / build clean.** Live E2E (controller-run 2026-06-30): 28/28 assertions on real `/api/mcp` StreamableHTTP endpoint. Deferred: UI, bulk/multi-doc ops, office-doc editing. **✅ Final whole-branch review DID run** — it returned two Minor findings (hoist `update_document`'s prior-snapshot above `updateDoc`; correct the handover's claim that the MCP parity test hardcodes 29 when it is dynamic), both fixed in `f19adfb`. This row and the handover's frontmatter both said "PENDING" for five weeks afterwards; corrected 2026-08-05 by task audit. These tools are long since live in prod (cycle 52 rebuilt half of them; `tools/list` returns 38). | ✅ shipped | [spec](../specs/2026-06-30-agent-document-tools-design.md) | [plan](2026-06-30-agent-document-tools.md) | [handover](../../handovers/2026-06-30-agent-document-tools.md) |
| 41 | **Agent-Loop Audit (bugfix cycle)** — live incident ("same reply repeated, tools at bottom, agent seems lazy") root-caused to THREE harness bugs, model exonerated (prod rows were distinct + it re-searched honestly): (1) **MDC streaming cache-key collision** — `<MDC>` keys useAsyncData on hash of the FIRST delta, so same-opener replies mirrored each other → per-entry `id` keys the v-for AND `MdView :cache-key`; (2) **tool chips now render INLINE** at stream position (WS `tool` events → `role:'tool'` transcript entries + undo; resume rebuilds from `tool_calls`; bottom SSE chips block removed); (3) **SearXNG silent blackout** — agent's own query burst rate-limited engines, defaults bench them 1h–24h, provider discarded `unresponsive_engines` → `{results, warning?}` surfaced to model + prompt rule + prod config (bing/mojeek/qwant, 60–300s suspensions; verified 0→20 results). Plus: `temperature` 0.7 pinned (anti copy-loop), maxSteps 6→12, anti-sycophancy + no-fake-narration prompt rules. Probe + live-chat browser-verified; **typecheck 0 / test 709 / build clean.** Deferred: structural tool-history (model still never sees its own prior tool calls — next agent cycle). | ✅ shipped (local; not yet pushed) | — | — | [handover](../../handovers/2026-07-01-agent-loop-audit.md) |
| 42 | **Always-Armed Agent + Memory Injection + Subagents** — Tony-directed follow-up to the cycle-41 audit. (1) **Always armed**: the dual-enable exec lever is GONE — one `bridgetProfile` (agentTools + execTool + subagents), `powerfulProfile`/`profileById`/`effectiveTools`/`ctx.execEnabled` deleted, "Powerful tools"/"Exec enabled" switches + cookie removed, old WS frames silently ignored; safety = the approval gate (dangerous → allowlist-or-approve; no-approval-UI channels auto-deny; fail-closed non-root runner). (2) **Proactive memory injection**: `buildMemoryContext` (top-5 relevant memories, floor 0.2, 1.5s timeout, never throws) injected per turn at the WS boundary; live context now rebuilt EVERY turn. (3) **Specialist subagents**: `makeSubagentTool` → nested `runAgent` with narrow toolset + own steering prompt + own step budget (new `ctx.maxSteps`); `research_web` (web tools, 10 steps, cited digest) + `search_brain` (11 read tools, 8 steps, digest w/ paths + gaps); fixed types, no recursion by construction, profile-only (invisible to MCP, test-guarded). Prompt: DELEGATE rule + always-on SHELL block + exact date/time line + cutoff steer. **typecheck 0 / test 718 / build clean**; browser E2E PASS (toggles gone; search_brain chip "(5 tool calls)"; exec approval → fail-closed on dev). Deferred: structural tool-history (→ cycle 43), executor subagent, subagent progress streaming. | ✅ shipped (local; not yet pushed) | — | — | [handover](../../handovers/2026-07-01-agent-always-armed-subagents.md) |
| 43 | **Structural Tool-History** — closes the two seams (live in-connection + resume) where the agent's model history was `{role, content}` text only, so it never saw its own prior tool calls/results across turns — the root cause behind cycle 39's `[image]` imitation and cycle 49's zero-tool-call "Done" ×2 fabrication. New pure `server/lib/agent/tool-history.ts` (`AgentToolRecord`, tiered decay: **calls survive forever, PAYLOADS decay after the last 3 tool-bearing turns** — `read` results capped 1500 chars replay / 8192 write, `create`/`destructive` results kept whole, `args` capped 1024 replay / 4096 write) + paired call/result block expansion (`toolBlocksFor`), captured at the `buildAiTools` `execute` seam (denials + thrown errors captured too, so a refused command doesn't get re-proposed) and replayed at **one call site** inside `runAgent` (`buildModelMessages`) so live and resumed history structurally cannot diverge. Persists additively onto the existing `conversation_messages.tool_calls` jsonb — **no migration**; legacy rows degrade to shape-only. Resumed attachments rehydrate for the same 3-turn window with no placeholder text (extends the cycle-39 invariant); resumed tool chips render at their true inline `textOffset` position, matching the live stream order. **Six defects caught in the PLAN itself** (not the build) — four were assertions/checks that could not fail (a fixture holding no URL, a `textOffset`-drop mutation that was a silent no-op on an under-sized fixture, a dead regex that could never match the real marker text, and untested resume-split logic that gave the gates zero signal), caught only by the plan's mandatory mutation-check steps or reviewer hand-trace while the test suite stayed green throughout; the other two were real bugs in the brief's own mandated code (a floating empty reply bubble on a trailing tool chip; offset-less records silently vanishing when mixed with offset-bearing ones). Spec Testing items 3 and 6 are now superseded by human ruling — see the handover. Built subagent-driven (9 tasks), then a **whole-branch review + one fix wave** (21 commits total; gates measured at `0d84fb1`, the last code-bearing commit): **typecheck 0 / test 1049 across 140 files / build clean.** Browser validation done via real DB fixtures driven through the real `resume()` path (dev/8004 homelab AI backend unreachable from the sandbox — pre-existing LAN-visibility limitation, cf. cycle 49); a live end-to-end turn is still unverified. **The whole-branch review found 1 Critical + 4 Important that nine clean per-task reviews could not** — each spans two tasks' seams: (1) tool-call `args` were capped nowhere and decayed never, so a 60 KB `save_document` body was persisted and re-sent on EVERY later turn, un-elidable → context overflow (the design's "a call costs ~50 tokens" was false for every write tool; now two-tier capped like results, 4096 write / 1024 replay / elided out-of-window, call always kept); (2) `textOffset` was recorded pre-sanitization while resume slices post-sanitized text, so a resumed chip landed mid-word ("Okay. D" | chip | "one.") — Task 8's browser validation used hand-authored offsets and structurally could not catch it; (3) `args` bypassed `redactForLog`, writing a refused `exec`'s unmasked command permanently into Postgres and shipping it to the browser; (4) the PARITY test never JSON round-tripped, so it was blind to the live/resume asymmetry its name claims; (5) `applyHistoryPolicy` threw on a malformed record, and `/api/agent/chat.post.ts` feeds it an unvalidated client body. Fix wave also closed the deferred untested `resume()` slicing (extracted to `app/lib/agent/transcript.ts`, 8 unit tests — two of which were mutation-insensitive on the first draft and had to be strengthened). **NOT merged, NOT pushed, NOT deployed** — a ✅ **LIVE-VALIDATED on prod by Tony 2026-08-07 ("working well")** — the real capture → persist → resume → replay round trip is confirmed against a live model turn, which is the one thing the build itself could not prove (the AI rig is unreachable from the dev sandbox; prod shares its LAN). All three follow-ups are now fixed and deployed in `3d96675` + `bc6a5ce`: the exec result-channel leak (`03957401`), the non-monotonic resume cursor (`fa2a9e0e`), and the `[attachment unavailable]` marker (`4ef76235` — which turned out to be three defects, one of them in this cycle's own resume-path strip). | 🔨 built (unmerged) | [spec](../specs/2026-08-05-structural-tool-history-design.md) | [plan](2026-08-05-structural-tool-history.md) | [handover](../../handovers/2026-08-05-structural-tool-history.md) |
| 44 | **Local AI Analytics (`/analytics`)** — Grafana-style read-only dashboard for the local AI estate, riding the homelab's EXISTING monitoring (Prometheus 3.7.3 @ 192.168.2.90:9090 — discovery found it already scrapes nvidia_gpu_exporter, vLLM, TEI, llama.cpp, litellm-exporter, blackbox; MyMind adds no collectors/storage). Server-side **named PromQL catalog** (client can only name panel ids; `Object.hasOwn`-guarded) + 3 endpoints (`snapshot`/`series`/`requests`) + encrypted `analytics_config` doc (`/settings/analytics`: URLs, write-only LiteLLM master key, editable gpuLabels uuid→name). UI: health strip, 5 GPU tiles, 12 Unovis panels (single-palette, dark `--vis-*` theming, null gaps = line breaks), LiteLLM request-log table (409→configure prompt). **vue-query polling; explicitly NO SSE** (external data). Homelab side: added missing `vllm-vision` :8005 scrape job (LXC 111) + documented the whole monitoring stack in the homelab doc. Review loop caught+fixed: proto-key panel bypass, Unovis null→0 zero-dips, and a live LiteLLM `/spend/logs/ui` contract mismatch (UTC datetime window required). No migration. **Built subagent-driven (10 tasks, two-verdict reviews); typecheck 0 / test 757 / build; FULL live E2E vs the real homelab (real master key, real rows). Merged + DEPLOYED to prod (master e28510d, run green, /analytics live + auth-gated, 2026-07-06); GPU label seeds corrected to Tony's live service map (PNY=Image Gen/ComfyUI, Zotac=voice stack).** | ✅ shipped | [spec](../specs/2026-07-06-local-ai-analytics-design.md) | [plan](2026-07-06-local-ai-analytics.md) | [handover](../../handovers/2026-07-06-local-ai-analytics.md) |
| 45 | **Agent Reasoning Block + On-the-fly Model Selector** — Two `/agent` additions (WS pipeline only), spun out of the cycle-45 prod bulk-usage investigation (Qwen3.6's `openai/`-prefixed LiteLLM alias emits `reasoning_content` → null-content under a tight token cap → `extractContent` throws, rescued by the no-think fallback; root-caused live, Tony fixed the bulk config). (1) **Reasoning "Thinking" block**: `run.ts` yields a `reasoning-delta` AgentEvent from the AI SDK `fullStream` (previously dropped) → orchestrator emits a `reasoning` VoiceEvent (**never TTS'd, never in `assistantText`/model history**) → `ws.ts` accumulates + persists it on `conversation_messages.reasoning` (migration **0026**, nullable) → client renders a collapsible `AgentReasoningBlock` (`<details>`, auto-open-while-thinking) above the answer, hydrated on resume. **Invariant: reasoning is display/storage only — `getAgentHistory` stays role+content, so it never re-enters the prompt.** (2) **Ephemeral reasoning-model override**: navbar `USelectMenu` (reasoning chain + "Default") → cookie `agent-model` → WS `{type:'model',modelDefId}` frame → pure `reorderChain` (chosen primary, **rest kept as failover**; unknown id = no-op). Connection-level, **never writes `ai_config`**; subagents don't inherit it. **Built subagent-driven (6 impl tasks, per-task two-verdict review + opus whole-branch review = ready-to-merge); typecheck 0 / test 762 / build; full browser E2E on dev (Thinking block persists 214 chars + survives resume; override proven via `activity_log.model_id`). Browser E2E caught+fixed a Critical dropdown bug typecheck/build/review all missed — reka-ui `ComboboxItem` rejects an empty-string value, so the `value:''` "Default" option crashed the popover; fixed with a `'__default__'` sentinel.** Built on `feat/agent-reasoning-model-selector`; **NOT yet merged/pushed/deployed** (awaiting Tony). | 🔨 built (unmerged) | [spec](../specs/2026-07-10-agent-reasoning-and-model-selector-design.md) | [plan](2026-07-10-agent-reasoning-model-selector.md) | [handover](../../handovers/2026-07-10-agent-reasoning-model-selector.md) |
| 46 | **Session ↔ Project Reassignment + Path-Based Auto-Routing** — Closes the cycle-23 gap where no-git-remote sessions with no label match were stuck in `uncategorized` forever with no way to move them. New `projects.path_prefixes text[]` (migration **0027**) — routing roots, distinct from the passively-accumulated `local_paths`. `findOrCreateProject`'s no-remote branch reordered: **longest `path_prefixes` match → cwd/git-root label match → auto-create (stoplisted) from the cwd leaf, seeding `path_prefixes` → Uncategorized fallback.** Pure helpers (`normalizePrefix`/`basenameOf`/`isUnderPrefix`/`longestPrefixMatch`/`isAutoCreatable`) in `server/lib/projects/path-routing.ts`, unit-tested. `cc-hook.sh` now sends `git_root` (transient label candidate, never persisted). **Reassignment**: `reassignSession`/`reassignSessions` (single/bulk) move a session, **cascade its `scope='agent'` memories**, and optionally register a `path_prefix` — teaching the router; `PATCH /api/sessions/[id]` / `POST /api/sessions/reassign`; a shared `ReassignProjectModal` wired into the sessions detail (Move button) and list (bulk checkbox select) pages. Hostname surfaced on sessions list/detail + a hostname filter. One-time `scripts/reresolve-uncategorized.ts` backfill (existing projects only, never auto-creates; dev dry-run: 14/399 would move). **Built subagent-driven (8 tasks); typecheck 0 / test 773 / build clean. Full playwright-cli + authenticated-fetch E2E PASS (hostname filter, single+bulk reassign, 15-memory cascade proven, prefix registration, reka-ui picker 0 console errors). Browser E2E caught+fixed a Critical bug (`046ddc9`) missed by typecheck/build/review: a component in `components/sessions/` referenced by its bare tag name silently fails to resolve (Nuxt auto-import gives it the dir-prefixed name) — both pages needed an explicit import.** Built on `feat/session-project-reassignment`; **NOT yet merged/pushed/deployed** (awaiting final whole-branch review + Tony's merge decision). Prod re-resolve backfill + Terawulf-cluster drain remain as post-merge operational steps. | 🔨 built (unmerged) | [spec](../specs/2026-07-15-session-project-reassignment-design.md) | [plan](2026-07-15-session-project-reassignment.md) | [handover](../../handovers/2026-07-15-session-project-reassignment.md) |
| 47 | **Knowledge Galaxy (`/galaxy`)** — Interactive 3D three.js knowledge graph of the whole second brain. **~1,907 nodes / 2,850 edges**: memories/documents/images/sessions + project hubs, POSITIONED BY MEANING via a single seeded **UMAP** projection (`umap-js`, in-Nitro) of the shared 2560-dim `halfvec` space (docs use **mean-pooled chunk vectors**; hubs = member centroids), cached in **`graph_layout`** (migration **0028**, table-only) by a nightly job (`compute-graph-layout`, `0 4 * * *`) + `POST /api/graph/recompute`. **Edges = STRUCTURAL** (membership→hub, memory→session provenance, doc→image ocr, memory↔memory supersedes/contradicts from `memory_relations`); similarity is on-demand ("Show similar" kNN highlight), not persistent lines. **Full CRUD + draw-relation** from a detail pane (memory edit/archive+undo, draw supersede/contradict+undo, session reassign via cycle-46 modal, image/doc delete, +New memory; heavy edits deep-link out), all riding the cycle-21 live bus (**700ms-debounced** `['graph']` invalidation). Custom **quaternion arcball** camera (grabbed node tracks cursor) + **drag-throw inertia** + spring-eased control panel (defaults retuned to Glow 0.35 / Node size 0.55 / Spread 1.2 for real scale — prototype's 1.0/1.0 bloomed to white at 1.6k memories). New endpoints: `GET /api/graph`, `/neighbors`, `/recompute`, `GET`/`PATCH /api/memories/[id]`, `POST`/`DELETE /api/memory-relations`. **Built frontend-first, subagent-driven (15 tasks / 3 phases + 2 fix waves + retune); per-task two-verdict reviews + final opus whole-branch review = Ready to merge (0 Critical). typecheck 0 / test 796 / build; Phase 1/2/3 playwright-cli E2E live-passed.** Subsumes task e356a621 (surface memory_relations). **MERGED to master (local ff) + PUSHED → CD deploy. ⚠️ post-deploy: `graph_layout` is empty on prod until the cron runs or a recompute is triggered.** Deferred fast-follows: incremental (non-full) refetch; off-thread recompute if ever UI-exposed. | ✅ shipped | [spec](../specs/2026-07-15-knowledge-galaxy-design.md) | [plan](2026-07-15-knowledge-galaxy.md) | [handover](../../handovers/2026-07-16-knowledge-galaxy.md) |
| 48 | **MCP OAuth — claude.ai custom connector** — MyMind's MCP server (`/api/mcp`) is now addable as a claude.ai/Desktop custom connector via a full OAuth 2.1 provider built on the **better-auth `mcp` plugin** (open DCR, PKCE S256 required, 1h access / 30d refresh tokens): three root **`/.well-known`** metadata routes (RFC 8414 + RFC 9728, `resource` byte-matches `<origin>/api/mcp`), middleware accepts OAuth bearers on `/api/mcp` **with expiry enforcement** (`isOauthTokenLive` — better-auth's `getMcpSession` doesn't check!) + RFC 9728 challenge on every 401, tables `oauth_application`/`oauth_access_token`/`oauth_consent` (migration **0029**), consent page `/oauth/consent` + login OAuth-flow resume. **Two security hardenings beyond the spec, both adversarially verified:** `oauth-consent-guard.ts` forces exact `prompt=consent` on every authorize (better-auth silently mints without it — connector-hijack vector; 23-input adversarial pass incl. `prompt=none`/dupes/case + login-resume cookie path) and 404s the public `get-session` route (leaked `refreshToken` rows). **Built subagent-driven (7 tasks + 3 fix waves); per-task two-verdict reviews + final fable whole-branch review = READY (2 findings fixed pre-merge incl. live expired-token 401 proof). typecheck 0 / test 817 / build; E2E DCR→PKCE→consent→token→MCP proven 4× via `scripts/mcp-oauth-e2e.mjs` + playwright-cli.** ⚠️ before any better-auth bump see task 66e9d7c9 (guard coupling = silent-no-op risk). Follow-ups there too: cleanup cron, consent UX polish. | ✅ shipped | [spec](../specs/2026-07-17-mcp-oauth-connector-design.md) | [plan](2026-07-17-mcp-oauth-connector.md) | [handover](../../handovers/2026-07-17-mcp-oauth-connector.md) |
| 49 | **Agent self-model hardening — Phase 1** — Three bleed-stops for the `/agent` loop, from prod conversation `054f2560` (2026-07-23): the agent fabricated "Done" **×2 with zero tool calls**, burned **~14 exec calls** rediscovering its own DB topology (assumed sqlite, tried the build-baked `@db` host), then **stopped mid-turn** on a `<tool_call>` streamed as text. (1) **Honesty invariant** (`prompt.ts`) — never report a mutation done without a tool result this turn; never assert unverified data facts. (2) **Environment self-model** (`prompt.ts`) — native systemd `mymind` (root) in **LXC 114**, DB = docker `mymind-db` (not sqlite / not `@db`), source+docs at `/opt/mymind` readable via `exec`. (3) **Tool-call-as-text recovery** (`run.ts`) — a `<tool_call>`/`<function=` marker streamed as text with **no real call** (local Qwen + vLLM streaming hermes bug [vllm#31871](https://github.com/vllm-project/vllm/issues/31871), which the cycle-45 `4d485c0` forced-final guard misses because text *was* emitted) triggers one re-run **WITH tools** + a corrective nudge (`reasoning:agent-recovered-textcall`), distinct from the no-text `agent-forced-final`; `!sawToolCall`-gated to avoid prose false-positives. (4) **`edit_project` `aliases` + `newSlug` rename** — the transactional slug cascade (`sessions`/`tasks`/`memories`/`documents.project`) already lived in `updateProject`; tool-surface + undo only. **Built subagent-driven (5 tasks + 1 final-review cleanup; per-task sonnet reviews + opus whole-branch review = Ready to merge, 0 Critical/Important). typecheck 0 / test 826 / build.** Deferred: recovery is single-step (add `stopWhen: stepCountIs(2)` so a recovered *real* call can also summarize — top Phase-2 item); marker-strip from stored text; vLLM bump (Ops). **MERGED to master (ff) + PUSHED → CD run 30092847426 green (test + deploy), prod `/api/health` 200 post-cutover.** ⚠️ prod E2E still pending: re-run the neo4nls cleanup on `/agent` to prove the fixes end-to-end at runtime (the schema-level tool test doesn't exercise the rename cascade). **Phase 2 = the Agent Skills subsystem (spec §2) — own plan next.** | ✅ shipped | [spec](../specs/2026-07-23-agent-skills-self-model-design.md) | [plan](2026-07-23-agent-self-model-hardening-phase1.md) | [handover](../../handovers/2026-07-23-agent-self-model-hardening-phase1.md) |

| 49b | **Agent Skills subsystem (Phase 2)** — Progressive disclosure for the `/agent` loop: detailed how-to guides live as **documents** (`documents.type='skill'`, reserved path `/projects/mymind/skills/<name>.md`, contract in the existing `frontmatter` jsonb — **no migration, no new table**), surfaced to the model as a cheap **Tier-1 index** (name + description + when-to-use, ~100 tok each) and loaded on demand via **`use_skill`**. The agent maintains them itself: `create_skill`/`edit_skill` are **kind `create` (ungated, deliberate)** and `delete_skill` is destructive; agent-authored skills go live **immediately** — no approval gate — with safety = structural validation + undo + activity-log audit + an **`agentSkillsEnabled` kill-switch** (`settings` key, default on; off ⇒ index omitted + `use_skill` refuses). `notSkill()` keeps skills out of `searchDocs`/`searchPassages` (the `isNull(type)` branch is load-bearing — `ne(NULL,'skill')` is NULL, so a naive `ne()` would hide **every** document). REST at `/api/skills/*`; UI at **`/settings/skills`** reusing the document editor + a nav entry; six seed skills via idempotent `scripts/seed-skills.ts`. **Prompt effect, measured:** base 6187 → **5324** chars (web-research detail moved into the `web-research-etiquette` skill), assembled-with-6-skills **7361** — i.e. the always-on prompt is *bigger* right now; the win is ~2k of pointers standing in for ~40k of procedure, and ~100 tok per additional skill. **Built subagent-driven (9 tasks + 1 final fix wave); per-task sonnet reviews + opus whole-branch review; 2 Important findings caught+fixed mid-flight (createSkill name-trim mismatch; unguarded `Body.parse` → 500) and 3 more in the final wave (wiki documented a nonexistent endpoint + nonexistent UI; no deploy step for the seeds; `list_documents` dumped every skill body and bypassed the kill-switch). typecheck 0 / test 850 / build; UI browser-verified 6/6.** **MERGED to master (ff) + PUSHED → CD run 30122394217 green (test + deploy); prod `/api/health` 200; six seed skills installed on prod via the public REST API (SSH was auth-denied, so `scripts/seed-skills.ts`'s on-box form was bypassed — the seed data is identical).** ✅ **AGENT ACCEPTANCE TEST PASSED ON PROD (2026-07-24):** asked prod `/agent` "safe way to rename a project slug?" — it reproduced the `db-maintenance` skill's dual-reference/slug-cascade trap verbatim, content that exists ONLY in the skill body (not the prompt), proving the local Qwen actually calls `use_skill` and answers from the loaded body. The dev-machine `ENETUNREACH` to the rig was a LAN-visibility artifact; prod shares the rig's LAN. | ✅ shipped | [spec](../specs/2026-07-23-agent-skills-self-model-design.md) | [plan](2026-07-24-agent-skills-subsystem-phase2.md) | [handover](../../handovers/2026-07-24-agent-skills-subsystem-phase2.md) |

| 50 | **Session search + transcript read (MCP tools)** — MyMind ingests every Claude Code session (messages + tool events, project-associated) and already ran hybrid search over them for the web UI, but none of it was on the MCP registry and there was no LLM-safe way to READ a transcript after a hit (`getSessionMessages` is unbounded; sessions reach ~200 msgs, single messages up to 280k chars). Four new **`kind:read`** tools on the shared `agentTools` registry (auto-exposed via `server/lib/mcp/server.ts` + gained by the in-app agent): **`search_messages`** (hybrid trigram+vector, match-centered `snippetAround` snippet, **always excludes sidechain**), **`search_sessions`** (title+summary topic search), **`read_around_message`** (focal ± `radius` turns, tool events interleaved, 404-shaped), **`read_session`** (paged transcript + meta + `hasMore`). New `server/services/session-read.ts` = a unit-tested pure core (`snippet`/`truncate`/`interleave`, caps `CONTENT_CAP` 2000 / `TOOL_CAP` 600 + `truncated` marker + `full` opt-out) + two bounded DB reads that merge `messages`+`tool_events` chronologically. `session-search.ts` gained backward-compatible `project`/`session`/`includeSidechain` filters + `*ForAgent` hydration wrappers (web DTOs in `shared/types/search.ts` untouched; the two `search.ts` callers updated). **No migration, no UI, no mutations.** **Built subagent-driven (5 tasks + 1 mid-flight fix + 1 final-review fix); per-task sonnet reviews + opus whole-branch review = Ready to merge. typecheck 0 / test 861 / build; live MCP round-trip proven (tools/list → search_messages → read_around_message chained).** Final review caught + FIXED a real ordering bug: `messages.createdAt` is set at INGEST (batched inserts share one `now()`) so ~1% share a `(session_id, created_at)` (worst cluster 71) — no tie-break meant `read_session` paging dup/skip and `read_around_message` DROPPING the focal's same-timestamp siblings; fixed with a `(createdAt, id)` composite cursor (determinism, not necessarily true order — that lives in `parentUuid`), probe-verified on the real 71-cluster. **MERGED to master (ff) + PUSHED → CD run 30382810386 green; prod `/api/health` 200; all 4 tools live on the prod MCP (37 total, matching the guard).** ✅ **PROD round-trip proven (2026-07-28):** a *semantic* `search_messages("navmesh pathfinding")` returned real Unity-NavMesh session hits — proving the **vector lane** runs on prod (dev shell couldn't reach the embedding rig) — and a returned `messageId` chained into `read_around_message` → 7-item chronological window, focal present. Nothing seeded (reuses existing tables). | ✅ shipped | [spec](../specs/2026-07-28-session-search-mcp-design.md) | [plan](2026-07-28-session-search-mcp.md) | [handover](../../handovers/2026-07-28-session-search-mcp.md) |
| 51 | **MCP recall hygiene** — Four independent fixes to the MCP recall path, filed from an external agent session's feedback plus an independent registry audit. (1) **Payload-shape fix (the load-bearing change, not just a row limit)** — `list_documents`/`search_docs`/`search_tasks`/`search_projects` gain body-free summary DTOs (`shared/types/summaries.ts`: `DocumentSummaryDTO`/`TaskSummaryDTO`/`ProjectSummaryDTO`, no `content`/`description`/`aliases`/`localPaths`/`pathPrefixes`) + a `{ items, total, hasMore }` envelope (`server/lib/agent/paging.ts`, `limit` default 25/max 100, `offset`) — deliberately `limit`/`offset`, NOT the cycle-50 `(createdAt,id)` cursor, since `search_docs` is relevance-ordered (RRF) and a date cursor can't express a position in a ranking. **Breaking MCP contract change:** `list_documents`/`search_docs` no longer return `result[].content` — bodies come from `get_document`/`read_document`/`grep_document`, and tool descriptions say so. (2) **Review-gated recall** — `search_memories`/`get_recent_memories` default to excluding unreviewed memories (`includeUnreviewed?` opt-out), sharing a new `reviewedCondition()` with `listMemories`; the automatic per-turn injection `buildMemoryContext` (fires on EVERY voice turn, no agent decision behind it, so no opt-out) also passes `reviewed: true`. (3) **Corroboration gate on the branch that actually archives** — `chooseResolution` (`memory-resolve.ts`) gains `scope`/`challengerSessions`/`sessionsFor(existingId)` + a new `review-contradict` action, and a shared `gatedByCorroboration()` now guards **both** the `refines` and `contradicts` branches when a user-scope claim, or a 2+-session-corroborated incumbent, is challenged by a less-corroborated single session. **Correcting the spec's premise:** `contradict` NEVER archived anything (verified against `e4f5a9e:memory-resolve.ts` — relation insert + review row + publishChange, nothing else), so `review-contradict` is byte-identical in effect and the gate as first shipped was **inert**. The branch that archives is `refines`→`supersede`; the gate now covers it, evaluated **before** the confidence comparison so a user-scope or out-corroborated refinement routes to `review-supersede` even at confidence 1.0 (mutation-tested: moving the gate after the comparison kills 7 tests). Needed **no migration** (`evidence`/`sessionId` already existed, `mergeEvidence` already appended per-derivation). (4) **Path collapse** — `shouldRecordLocalPath`/`collapseLocalPaths` (`server/lib/projects/path-routing.ts`, built on cycle-46 prefix helpers) stop `localPaths` re-accumulating entries a `pathPrefixes` entry already covers, plus a dry-run-by-default one-time cleanup script (`scripts/collapse-local-paths.ts`, `--project <slug>` filter, prints up to 200 dropped paths so a real prod run is fully auditable, **no undo — snapshot first**) for existing bloat (Terawulf, prod-only — **not yet run**). **Rejected as a non-goal:** tiered/per-project sensitivity scopes on MCP tokens (Tony: connector is single-operator, intentionally unscoped — not an oversight). **Same-corpus acceptance measurement** (62 docs/68 tasks, identical rows both sides): `list_documents` 394,334→5,532 chars, `search_docs` 300,780→5,528, `search_tasks` 117,270→4,616 — `list_documents`'s true reduction is **~97-98%**, not a flat 98.6%, since its "before" count includes 6 skill-type docs the old tool only filtered in JS after fetching; prod baselines (662,712/472,472/282,904) are a different, historical corpus, never blended in. Also corrected a real **spec/plan defect** (not a build defect): `search_projects` was assumed to wrap an existing `searchProjects(q)` that never existed anywhere in the repo — shipped instead as a paged lister only (`activeOnly`), no invented text search, after a stopgap substring-match implementation was built then deliberately dropped on review. **Built subagent-driven (8 tasks + 2 mid-flight plan-defect fixes, per-task review + fix rounds, then a whole-branch review — verdict MERGE WITH FIXES — and a final ruled-on fix wave); typecheck 0 / test 934 (133 files) / build clean.** Wiki `memory.md` (resolution ladder + gate + recall defaults) and `projects.md` (`local_paths` collapse + script runbook) updated in the same change. **Fifth planning defect, the most consequential:** the spec asserted `contradict` auto-archived a memory; it never did, so item 3's first gate shipped inert — read the handover's "Item 3: the spec's premise was wrong" before touching `memory-resolve.ts`. **MERGED to master (ff `e4f5a9e..dc06f1e`) + PUSHED → CD run 30573162535 green; prod `/api/health` 200, verified in-container.** ✅ **PROD ACCEPTANCE (2026-07-30):** live MCP round-trip against the REAL prod corpus — `list_documents` 662,712→**6,187** chars (-99.1%), `search_tasks` 282,904→**5,935** (-97.9%), both `items:25` / `hasMore:true` (85 docs / 157 tasks), programmatically confirmed no `content`/`description` on any item. ✅ **`collapse-local-paths.ts --apply` WAS run on prod (2026-07-31)** — terawulf applied alone first as a smoke test (62→0, `path_prefixes` untouched at 4, re-run 0 changes), then the remainder; **`local_paths` across all projects 194 → 17**, full re-run `0/35 changes` (idempotency confirmed live). Snapshots on LXC 114: `/root/local_paths.bak.csv` (pre-smoke-test) and `/root/local_paths.preapply.csv` (pre-apply, incl. `path_prefixes`). This row previously said "NOT yet run" — corrected 2026-08-05 by task audit against prod + the MyMind handover mirror, which had recorded the completion the repo copy never picked up. Safe because `local_paths` is written-only apart from one read in `shouldRecordLocalPath`; routing uses `pathPrefixes`/`gitRemoteKey`/aliases and never `local_paths`. Follow-up: `tsx` is used by that script but declared in neither `dependencies` nor `devDependencies` (resolves transitively only). | ✅ shipped | [spec](../specs/2026-07-29-mcp-recall-hygiene-design.md) | [plan](2026-07-29-mcp-recall-hygiene.md) | [handover](../../handovers/2026-07-29-mcp-recall-hygiene.md) |

| 52 | **MCP agent ergonomics — write receipts + `sync_document`** — Filed from an external Claude Code session's feedback (4 issues + smaller items); **two of the four did not survive investigation.** "`limit` default not applied" was a **deploy boundary, not a bug** — the reporter's two calls straddled cycle 51's 2026-07-31 deploy (before it, `search_docs` had no `limit` param at all and returned full bodies: 33 candidates × ~12 KB = their exact 398 KB). "Schema doesn't advertise implemented features" — nothing receipt-related existed anywhere, and `server/lib/mcp/server.ts:22` hands the handler's own Zod shape to `server.tool()`, so schema drift is **structurally impossible** here. (1) **Write receipts (a correctness fix, not a cost one)** — the six document write tools stopped echoing the body; on a large doc the response exceeded the MCP host's tool-result cap, so a write that had ALREADY COMMITTED reached the agent as an error → retry (double-apply / stale `old_string`) or false failure report. The same `exec.result` feeds the **in-app agent** (`ai-tools.ts:58`), so the echo was burning its context too; nothing consumes the body (the UI uses `exec.summary`), so receipts replace it outright — the report's proposed `return:"full"` mode was dropped as a foot-gun. Plus **typed edit failures** (`no_match` / `ambiguous_match` + candidate lines / `empty_old_string` / `not_found`). (2) **`sync_document`** — "make this doc match this file" in one call; files carry `mymind_id` + `mymind_hash` in frontmatter so it works identically for git repos, non-git dirs, and MyMind-native docs. **The hash covers the BODY ONLY** — a whole-file hash changes the moment you write it back and never converges; free here because `content`/`frontmatter` are separate columns (prod audit: only 3/103 docs embed frontmatter in `content`). Four actions (`created`/`adopted`/`updated`/`unchanged`), **fail-closed** refusals with a body-free divergence report, **gated adoption** so a first sync can't clobber a UI edit, rename-follows-id relocation, metadata passthrough, and a **probe mode** (`local_hash`, no body, never writes) because the real cost is the upload on days nothing changed. **`documents.content_hash` became a Postgres GENERATED column (migration 0030)** — a CAS on a hash any writer can desync isn't safe, and `image-enrich.ts:90` already bypassed `updateDoc` via a raw `db.update`. ⚠️ the naive generated expression is REJECTED (`convert_to` is not immutable); an explicitly-IMMUTABLE `doc_content_hash()` wrapper is required. The CAS guard lives in the `UPDATE`'s `WHERE content_hash = $expected`, not a preceding SELECT. **Built subagent-driven (Tasks 0–7 + a pre-flight plan-defect fix + 2 per-task fix rounds + a final whole-branch fix wave); per-task two-verdict reviews + opus whole-branch review = READY WITH FOLLOW-UPS, 0 Critical. typecheck 0 / test 992 (136 files) / build clean; live E2E 28/28 vs real Postgres through the real `/api/mcp`, incl. a genuine `Promise.all` CAS race (exactly one winner, the other `hash_mismatch`).** **Review caught two tests that proved nothing:** the plan's "CAS race" test never reached the CAS (`casUpdateContent` called 0×, leaving the safety-critical branch uncovered in 973 tests) and the first commit's "receipt stays small" test measured a 392-byte ERROR object (120,000 matches → `ambiguous_match`, so no receipt was ever built). Pre-flight also caught that the plan's real-DB tests would have **broken CI and blocked every deploy** (no Postgres service; `deploy` needs `test`) → Task 0 split them to `*.db.test.ts` / `pnpm test:db`. Prod pre-flight: 110 rows, **0 drifted** — the migration is a data no-op. **MERGED to master (ff `9004300..329f55b`) + PUSHED → CD run 30761974846 green; prod verified in-container: `/api/health` 200, `content_hash` `is_generated=ALWAYS`, 112/112 rows hash correctly, live `tools/list` → 38 tools with `sync_document` present.** ⚠️ deploy-window + code-only-rollback hazard from the generated column — see the handover. Follow-ups: the MCP preamble still prescribes the old edit-replay workflow and never mentions `sync_document`; error shapes remain inconsistent across the other doc tools; no CI Postgres. | ✅ shipped | [spec](../specs/2026-08-01-file-sync-primitive-design.md) | [plan](2026-08-01-file-sync-primitive.md) | [handover](../../handovers/2026-08-02-mcp-agent-ergonomics.md) |
| 53 | **MCP document-tool ergonomics — unified errors, guarded undo, honest preamble** — Closes cycle 52's three follow-ups. (1) **One failure shape everywhere**: `read_document`/`grep_document`/`edit_document`/`edit_section`/`update_document`/`move_document`/`delete_document`/`sync_document` all now answer `{ ok: false, error: <code> }` plus prose (`message`, or `hint` on `sync_document`'s three divergence errors, which carry no `message`); codes with pure-logic origins live in `server/lib/documents/edit-ops.ts` and are passed through verbatim, never re-spelled; each converted tool's `description` lists its codes; a heading failure returns the outline capped at `MAX_ERROR_OUTLINE` (50, `outlineTruncated` flag). `get_document` is the one deliberately-left unconverted outlier. (2) **Undo can refuse instead of clobbering** — `UndoFn` widened additively to `Promise<void | {ok, reason?}>`; `runUndo` consumes the token only on success, so a refusal stays retryable — and catches a throwing closure (a real risk: the unique index on live paths makes a path-restore throw), which otherwise escaped as a repeatable raw 500. All five document-mutating undo closures now guard: content restores CAS against the generated `content_hash` column; path/title/tags/type/domain/frontmatter (no CAS primitive) read-then-compare on `updatedAt` (documented, accepted TOCTOU). `sync_document`'s update branch also restores the five metadata fields its own `applySyncMeta` wrote (until the final fix wave it reverted the body only and still reported `ok:true`). `sync_document`'s create branch and the three retire tools (`delete_document`/`delete_task`/`forget_memory`) still restore unconditionally — untouched, no guard added. (3) **The refusal reaches the UI** — a pure `undoFeedback()` + one `useUndo()` composable wired into `agent/index.vue` and `galaxy.vue` (which had a **worse** bug this task fixed: an unconditional success toast that would have contradicted the new refusal toast, showing two opposite-outcome toasts on one refused undo). (4) **An honest preamble** — points at `sync_document`; after three review rounds of per-tool undo wording each turned out false for some branch, the final fix wave cut the undo clause entirely: `MCP_INSTRUCTIONS` is read only by MCP clients and they cannot invoke undo at all, so it now says "There is no undo tool here — get writes right the first time" (tests pin the wording and a ≤998-char budget). (5) **Curated titles survive a relocation** — `nextTitleOnMove` only re-derives from the new path's basename when the current title was itself auto-derived. **Ten production documents reconciled** (Task 10; the plan said three, from a stale cycle-52 audit) — walked all 110 documents in the corpus; two of the ten (`671334c0`, `44f44776`) open with a provenance blockquote *before* their `---` frontmatter block and would never match the plan's own `content LIKE '---%'` discovery query. **⚠️ Two things a future session must not assume:** the MCP transport itself never issues an undo token to any client — `buildMcpServer` (`server/lib/mcp/server.ts:22-26`) returns only `exec.result`, no `undo` tool exists, and `runUndo`'s only route (`POST /api/agent/undo`) is unreachable from an MCP session — so Tasks 2-5's undo hardening protects the **in-app agent surface only**; and `pnpm test:db`'s 33 CAS/guard tests are **not wired into the deploy gate** (MyMind task `70bcc740`, stays open) — `pnpm test` alone would stay green through a broken CAS. **Built subagent-driven (10 tasks, 5 fix rounds — Tasks 5/6/7/9 one each, Task 8 two); per-task review = clean, then one final whole-branch fix wave (see the handover's "Final fix wave" section) that fixed a partial `sync_document` undo reporting success, the dead preamble undo clause, a throwing undo escaping the `{ok,reason}` contract as a repeatable 500, and four false doc claims. typecheck 0 / test 1102 (143 files) / test:db 33 (4 files, manual) / build clean.** Eight plan defects caught during execution (none by a gate) — a fixture asserting the opposite outcome for identical inputs, an `?? null` that meant force-write (the clobber fix contained a clobber), an undo restoring two fields where the function patches five, a repo-wide invariant asserted from grepping one file, a parameter typed too narrowly to satisfy its own call site, a preamble replacement that recurred as a different false generalisation three times, an incomplete client file list, and a discovery query with a hidden byte-0 anchor assumption. **MERGED to master (ff `f16648f..aa9e068`, 25 commits — 19 code, 6 docs) + PUSHED → CD run 31387137325 green (test + deploy, cut over to native, DB-touching `/api/health` 200); prod verified after cutover — public `/api/health` 200 in 0.76s and an authenticated `search_tasks` MCP round-trip succeeded, exercising the auth middleware's `api_tokens` query and the DB on the new build.** Deferred past the deploy: the live smoke test of the undo-refusal UX (blocked on `/agent` rendering blank in dev, task `3201e7a4`), and the worst parked residual — `undo.ts:47` interpolating a raw `DrizzleQueryError`, and therefore an entire prior document body, into the user-facing reason — now filed as task `0ff0f619`. | ✅ shipped | [spec](../specs/2026-08-07-mcp-doc-tool-ergonomics-design.md) | [plan](2026-08-07-mcp-doc-tool-ergonomics.md) | [handover](../../handovers/2026-08-08-mcp-doc-tool-ergonomics.md) |
| 54 | **MCP SDK v2 migration — dual-era serving** — Moves the MCP server off the frozen `@modelcontextprotocol/sdk` v1 onto `@modelcontextprotocol/server` v2's `createMcpHandler`, serving both the 2025 protocol era and the new 2026-07-28 era from the same `/api/mcp` endpoint with no observable change for any current client (every live client today still opens with the 2025 `initialize` handshake). Tool registration moves from `server.tool()` to `server.registerTool()`, passing the existing `AgentTool.schema` `ZodRawShape` bare. The handler is constructed **once at module scope** in `server/api/mcp/index.post.ts` (it owns a subscription bus + `close()` lifecycle); `buildMcpServer()` stays the per-request factory, so per-request `McpServer` isolation is unchanged. `legacy` is left at its `'stateless'` default (never `'reject'`, which would sever every existing connector). `createMcpHandler` performs no Host/Origin validation of its own, and — contrary to this row's original wording — neither did the pre-v2 route: the v1 SDK offered `enableDnsRebindingProtection` as an opt-in defaulting to `false`, and the old `server/api/mcp/index.post.ts` never set it, so `/api/mcp` had **no** such validation before this branch. This branch adds it fresh via a new `mcpAllowedHosts()` allow-list (`server/lib/mcp/guards.ts`, fails closed to `localhost`/`127.0.0.1`/`[::1]` when unconfigured) gating every request ahead of `fetch` — **net-new hardening, not a restoration**. First transport-level tests for this endpoint: `test/mcp-transport.test.ts` (4 tests, real HTTP round-trip proving both eras negotiate and serve the real 38-tool registry) and `test/mcp-host-guard.test.ts` (12 tests, calling the guard functions directly since Node's `fetch` silently strips a forbidden `Host` header over a real round-trip). **Host and Origin use deliberately DIFFERENT allow-lists** (`bce3d9f`): Origin also permits `claude.ai`/`claude.com` so the cycle-48 OAuth connector's cross-site requests aren't 403'd, while Host stays narrow — adding Anthropic's domains to a single shared list would have accepted `Host: claude.ai` and reopened the exact rebinding hole the guard closes; the load-bearing test asserts `Host: claude.ai` still 403s. Three spec claims were falsified by spiking the real 2.0.0 packages *before* implementation (Nitro runs h3 **1.15.11**, not v2 — the `h3@2.0.1-rc.20` in the pnpm store belongs to another consumer; `registerTool` takes a bare `ZodRawShape` so no `z.object()` wrapper is needed; `authInfo` can't carry `event.context.client`), and the final review falsified a fourth (the "restored" framing — v1's `enableDnsRebindingProtection` defaults false and this endpoint never set it, so commit `40e08eb`'s subject line is wrong and the wiki is right). Review independently confirmed all 38 tool schemas byte-identical to `z.toJSONSchema` output, and probed 9 request shapes on real h3 — nothing 500s that previously didn't. typecheck 0 / test 1118 (145 files) / build clean. No migration, no schema, no persisted state. **MERGED to master (ff `00355c7..bce3d9f`, 9 commits) + PUSHED → CD run 31764558377 green; prod verified — `/api/health` 200, unauth `POST /api/mcp` 401 with the RFC 9728 pointer intact, and two REAL authenticated MCP tool calls (`search_tasks`, `search_memories`) from a live legacy-era Claude Code client succeeded against the migrated server.** ⚠️ The obvious smoke test cannot detect this branch's worst failure mode (an empty `betterAuthUrl` fails the guard closed and 403s everything; health is public and the unauth 401 fires before the guard) — only a real authenticated client call does. Follow-ups: the claude.ai connector still needs a live check against the new Origin guard; `index.post.ts` has no automated coverage (the transport test's `mount()` omits `parsedBody`); the `inputSchema` cast binds to a `@deprecated` overload; deferred — cacheable list results, the Tasks extension, and the DCR→CIMD OAuth track (its own cycle). | ✅ shipped | [spec](../specs/2026-08-13-mcp-sdk-v2-migration-design.md) | [plan](2026-08-13-mcp-sdk-v2-migration.md) | [handover](../../handovers/2026-08-13-mcp-sdk-v2-migration.md) |
| 55 | **Token & cost analytics — Usage tab** — Adds a **Usage** tab to `/analytics` (alongside the unchanged cycle-44 Infrastructure tab): historical token consumption and API-equivalent value across Claude Code sessions plus LiteLLM spend, with per-model breakdown, cache economics, and agent-dispatch composition. **No new collection** — built entirely on `messages.usage`, which the session ingest has carried since April; `messages`/`sessions`/`tool_events` stayed read-only for this cycle. Two new tables (migration `0031_zippy_random.sql`): **`model_prices`** (PK `model`, five `numeric` rate columns + `source`/`fetched_at`, synced nightly at `0 4 * * *` by `server/tasks/sync-model-prices.ts` from LiteLLM's public `model_prices_and_context_window.json` for every model actually seen — 8 rows on the local corpus) and **`litellm_daily`** (composite PK `(day, model)`, `tokens`/`spend`/`requests`, rolled up nightly at `20 0 * * *` by `server/tasks/rollup-litellm-daily.ts` from the same Prometheus series the live Infrastructure panels already query — a **deliberate, scoped exception** to the wiki's cycle-44 "collects and stores no metrics" rule, made specifically because that rule caps usable history at Prometheus retention; the wiki's own claim was reworded in the same change so the two no longer contradict). A pure, unit-tested `computeValue` (`server/lib/analytics/cost.ts`) prices six token classes — input/output/cache-read/ephemeral-5m/ephemeral-1h plus a **residual** term (`cache_creation_input_tokens` minus the two ephemeral splits, clamped ≥0, priced at the 5m rate) that exists because the real corpus has rows where the ephemeral split doesn't sum back to the reported total (3 of the 81,954 usage-bearing messages report a non-zero `cache_creation_input_tokens` — 3,028 each, 9,084 total — while both ephemeral tiers read 0). **Cache reads are ~95% of total tokens and price at ~10% of the input rate** — flagged as the single likeliest way a future change silently ~10×s the whole dashboard if that distinction is ever collapsed. A model absent from `model_prices` is skipped into an explicit `unpriced` bucket, never priced at $0 (`<synthetic>`, Claude Code's local-message marker, is the permanent example — currently invisible in the UI since its 181 rows carry all-zero token counts, so `unpriced.tokens` is 0 on the real corpus). The API-equivalent-value tile is explicitly labelled "at API rates — not billed" (Claude Code is subscription-billed); `litellm_daily.spend` is real money and renders as its own visually distinct, never-summed panel. Two new endpoints (`GET /api/analytics/usage?range=`, `GET /api/analytics/dispatches?range=`, both session/bearer-gated via the existing `server/middleware/auth.ts`, unknown range → 400) serve a **new, deliberately separate** `UsageRangeKey` (`7d|30d|90d|all`, daily Postgres buckets) alongside the existing cycle-44 `RangeKey` (`1h|6h|24h|7d`, Prometheus) — both spell `7d` but mean different things, and the page keeps two independent `ref`s rather than share one (a shared ref would typecheck and silently query the wrong granularity). Two new chart components (`UsageStackedChart`, generic `UsageBreakdownBars`) reuse the existing 8-hue `CATEGORICAL_LIGHT`/`CATEGORICAL_DARK` palette (copied verbatim from `TimeSeriesChart.vue`, which doesn't export it) with a hash-per-model + alphabetical-order linear-probe collision resolver, sourced from a canonical (`range=all`, gated to the Usage tab, 30-min-cached) model set so colours stay stable across range switches; with the real 9-model set against 8 hues one collision is pigeonhole-forced (`claude-opus-4-6`/`claude-sonnet-4-6` share a hue) and is disclosed rather than hidden. **Built subagent-driven (Tasks 1–7 of a 9-task plan, incl. 2 fix rounds on Task 7's colour-collision finding; Task 8 is this documentation pass; Task 9 — ship + verify — has not run); typecheck 0 / test 1136 (148 files), all green / `test:db` kept separately gated (not in the CI/deploy path) / build clean; live browser validation via playwright-cli (light + dark, all 4 usage ranges, colour stability across a range switch via `getComputedStyle` diffing, cross-tab range independence proven both directions).** Measured on the local corpus (`range=all`): **$25,267.88** API-equivalent value, **26.56B** tokens (**95.5%** cache reads), **2,671** agent dispatches across **454** sessions — independently cross-checked against a separate Python re-implementation of the pricing math (delta was exactly the residual term, i.e. the shipped figure is the *more* correct one). **MERGED to master (ff `a24eee4..985b635`, 15 commits) + PUSHED → CD run 31861147144 green (test ✅ / deploy ✅, the run that applied migration 0031); prod verified in-container** — both tables present, `drizzle.__drizzle_migrations` 31→**32**, existing data intact (**147,078** messages / **597** sessions), and a live authenticated round-trip on `/api/analytics/usage?range=30d` returned real aggregates (10.9B tokens, 96.76% cache reads, 83 sessions, 1,197 dispatches), `range=1h` → 400, dispatches breakdown live. ⚠️ **Expected cold start:** `model_prices`/`litellm_daily` are empty until their `0 4 * * *` / `20 0 * * *` crons, so the value tile reads **$0.00** right after deploy with every model in the unpriced bucket — and there is **no manual trigger in production** (`/_nitro/tasks` is dev-only; no admin endpoint). **Seven plan defects were caught during execution**, two of which would have shipped broken: GitHub serving `.json` as `text/plain` made the first price sync upsert **0 of 9 models**, and `destr` silently returning the raw string meant the first fix was insufficient. Two fix rounds themselves needed re-fixing — one had quietly added an unbounded full-history query to *every* `/analytics` load. Follow-ups: cheap `SELECT DISTINCT model` source for the chart palette, negative-clamp on the SQL token path, `toFixed(20)` decimal safety, palette triplication across three chart components, and no index on `messages.created_at`. | ✅ shipped | [spec](../specs/2026-08-14-token-cost-analytics-design.md) | [plan](2026-08-14-token-cost-analytics.md) | [handover](../../handovers/2026-08-14-token-cost-analytics.md) |
| 56 | **Home dashboard (`/`)** — Replaces the `/`→`/documents` redirect (which reopened the last-open markdown file as the app's front door) with a real dashboard: metrics strip on top, an interleaved "what happened" timeline in the wide left column, five actionable panels in a right rail (Needs attention, Quick capture, Ask the brain, Active tasks, Recent projects). One endpoint (`GET /api/home?range=`, `server/services/home.ts`) fans out six DB-backed panel queries in parallel and returns them as one payload — one round trip, one skeleton, one error state; rig health stays a separate self-fetching tile (`['analytics','snapshot']`, cycle 44) so a dead Prometheus degrades one tile, not the page. A pure, unit-tested `buildTimeline()` (`server/lib/home/timeline.ts`) buckets events by UTC day and type, collapsing `memory`/`document`/`image`/`clipboard`/`task`/`conflict` above 3-per-day into one summary row while `session`/`error` **never** collapse (the exact failure this page exists to prevent — `embeddings:all-failed` sat as a grey `41` in the sidebar for 9 days), then caps at 60 rows with disclosed truncation ("Showing X of Y"). `HomeRangeKey` (`1d|3d|7d|30d`, default `3d`, `mm.home.range` cookie) is a deliberate **third** range vocabulary alongside cycle 44's `RangeKey` and cycle 55's `UsageRangeKey` — all three spell `7d`, none mean the same query. **Needs attention is deliberately NOT range-scoped** (absolute backlog counts — conflicts/unreviewed/unacked/unfiled — verified byte-identical between `range=1d` and `range=30d` live in-browser while the timeline underneath changed completely). `/api/analytics/usage` 400s on `1d`/`3d`, so `getUsage()` (cycle 55) was split into a thin range-key wrapper over a new `getUsageSince(start: Date)` that Home calls directly — no shared range enum, `getUsage`'s own signature and callers untouched. New index `messages_created_at_idx` (migration **0032**) closes a sequential-scan-on-every-load gap cycle 55's handover had logged as a follow-up. `['home']` joins the cycle-21 live bus, trailing-debounced at 700ms (`HOME_DEBOUNCE_MS`, matching `GRAPH_DEBOUNCE_MS`) across nine of twelve `ResourceName` members. `/agent` gained a `?q=` param (from Home's "Ask the brain") that prefills the composer via a new optional `initialText` prop on `Composer.vue`. Originally prefill-only; the follow-up pass made it **auto-submit on arrival** at Tony's request, guarded so a URL cannot re-fire a model call — the page `router.replace`s `q` out of the URL (a *replace*, so `?q=` never enters history) and the composer fires at most once per distinct value. **Two plan defects caught during the build, both consequential:** (1) `/` was redirected in TWO places — the page component the brief named, AND a `'/': { redirect: '/documents' }` routeRule in `nuxt.config.ts` it didn't; a routeRules redirect pre-empts the page entirely, so the dashboard would have been unreachable even with the component rewritten. Found and fixed by the implementer, confirmed by Tony. (2) The brief specified `UButtonGroup` for the range switch; the installed Nuxt UI v4 renamed it `UFieldGroup` — swapped, plus a related onClick return-type typecheck fix. **Built subagent-driven (9 build tasks + this documentation task; 5 fix rounds across Tasks 4/5/6/8/9 — two of them, Task 4's soft-delete/unbounded-CTE pair and Task 8's needs-attention singular/badge pair, were Important findings on the plan's OWN prescribed code, escalated and fixed per Tony's ruling that plan-mandated correctness/scope findings get escalated while accessibility fixes in-loop and cosmetic ones get deferred-and-flagged); typecheck 0 / test 1164 across 152 files, all green / build clean.** Live browser validation via playwright-cli: 1280×800 layout confirmed (metrics strip / wide timeline / five-panel rail, no clipping); the range-invariance proof above; 390×844 — `horizontalScroll: false`, 0 page-level overflow (28 raw hits all traced to the deliberately horizontally-scrollable metrics strip, fully contained within its own bounds), 0 hover-gated interactive elements — explicitly checked against two regressions elsewhere in this app that Home must not repeat (Tasks' CTA clips ~20px off-screen at 390px; Clipboard's 16 Copy buttons are `opacity: 0` on touch). **Merged, pushed and DEPLOYED** (CD run 31924474991, which applied migration 0032; prod verified — `EXPLAIN` on home's query shape now reports Index Only Scan using `messages_created_at_idx`). A follow-up pass shipped in CD run 31951490660: icon rig badges + globally zero-delay tooltips, a richer projects card (documents + open-tasks counts and a relative age; open tasks deliberately NOT range-scoped), `?q=` **auto-send** (see below), the unpriced-tokens gate fix, and `getUsagePricingSince` — Home was running the full Usage-tab aggregation and discarding two-thirds of it, i.e. a SECOND `messages` scan on the landing page the 0032 index was added to avoid. Four adjacent audit findings deliberately left out of scope and filed as named backlog follow-ups: capture titling, sidebar IA, login deep-link preservation, the document editor's silent data loss. | ✅ shipped | [spec](../specs/2026-08-15-home-dashboard-design.md) | [plan](2026-08-15-home-dashboard.md) | [handover](../../handovers/2026-08-15-home-dashboard.md) |

> Also tracked in the backlog (not yet cycled): GitHub-commits→memory, video→webm, EXIF scrub, and assorted tech-debt. (Image semantic search shipped in cycle 20; **session-summarization, bridget data migration, and session/message search shipped in cycle 13**.) See [`docs/BACKLOG.md`](../../BACKLOG.md).

## Reference repos (read-only sources)

- `~/Documents/GitHub/codethis-dev` — Postgres document model, `public_slugs` sharing, language detection, Shiki + CodeMirror.
- `~/Documents/GitHub/bridget-services/command-center` — split file-tree/editor layout (Nuxt UI v4 `UDashboardPanel`), CodeMirror + MDC, edit/preview/split, the `fs`-seam to swap for Postgres.
- `~/Documents/GitHub/bridget-services/memory` — Python memory service: data model, two-stage dedup, hybrid RRF search, enrichment loop (design to port to TS).
- `~/Documents/GitHub/copipasta` — same stack (Nuxt 4 + Nuxt UI v4 + better-auth + Drizzle); storage abstraction, uploads, SSE, the Clipboard feature itself.
