# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

.claude/skills/*/cache/**
.claude/skills/*/manifest.json
# playwright-cli artifacts
.playwright-cli

# Personal scratch / test uploads — keep out of a public repo
IMG_*.jpg
IMG_*.jpeg
IMG_*.png
*.zip

# Git worktrees created for isolated feature work
.claude/worktrees/
.superpowers
docs/screenshots/voice-agent.gif

# SearXNG writes this on first boot — generated, not tracked
searxng/uwsgi.ini

# superpowers brainstorm visual companion
.superpowers/

# One-time script artifacts: memory/label dumps produced by scripts/*.ts.
# These carry raw memory CONTENTS — personal data, and in one case a live API
# key that GitHub push protection caught on the way to this PUBLIC repo.
# Keep them local; never commit them.
scripts/data/
